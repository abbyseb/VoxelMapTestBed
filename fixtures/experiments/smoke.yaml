experiment_id: exp_smoke_baseline
name: smoke_baseline
model:
  entry: models.baselines.concatenated:build
  kwargs:
    use_film: true
data:
  gold_pack: spare-gold-smoke-v1
  profile: smoke
  custom_scans: []
train:
  epochs: 20
  batch_size: 8
  lr: 1.0e-5
notes: |
  Fixture experiment for mock mode UI.
hypothesis: Baseline concat + FiLM on smoke profile.
